export const validateCredentials = (username, password) => {
    // Replace this logic with actual authentication mechanism
    // For example, you might call an API to check the credentials
    return username === 'testuser@gmail.com' && password === '123';
  };